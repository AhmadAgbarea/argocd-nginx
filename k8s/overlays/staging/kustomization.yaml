apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: nginx-example-staging

namespace: nginx-example-staging

resources:
- ../../base

patches:
- path: patch.yaml
  target:
    kind: Deployment
    name: nginx-example
- path: ingress-patch.yaml
  target:
    kind: Ingress
    name: nginx-example

commonLabels:
  environment: staging
  version: staging

images:
- name: nginx
  newTag: "1.25-alpine"
