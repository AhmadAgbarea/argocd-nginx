apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-example
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-staging"  # Use staging issuer
    external-dns.alpha.kubernetes.io/hostname: "nginx-staging.iqball.tech"
    external-dns.alpha.kubernetes.io/ttl: "300"
    traefik.ingress.kubernetes.io/router.middlewares: "nginx-example-staging-redirect@kubernetescrd"
spec:
  tls:
  - hosts:
    - nginx-staging.iqball.tech
    secretName: nginx-example-staging-tls
  rules:
  - host: nginx-staging.iqball.tech
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: nginx-example
            port:
              number: 80
